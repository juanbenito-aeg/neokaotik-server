enum Environment {
  TEST = "test",
  DEVELOPMENT = "development",
  PRODUCTION = "production",
}

enum Methods {
  POST = 0,
  PUT = 1,
}

enum ArtifactState {
  ACTIVE = "active",
  COLLECTED = "collected",
}

export { Environment, Methods, ArtifactState };
